import { containerStyles } from 'components/GlobalStyle';
import styled from 'styled-components';

export const MainContainer = styled.div`
  ${containerStyles}
  display: flex;
  flex-direction: column;
  align-items: center;
`;
